import React from 'react';

const AboutSlugs = async({params}) => {
    const p = await params
    console.log(p);
    return (
        <div>
            
        </div>
    );
};

export default AboutSlugs;